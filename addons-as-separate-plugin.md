# تبدیل افزودنی‌ها (Add-ons) به افزونه جداگانه — گزارش فنی

## وضعیت فعلی (بر اساس بررسی کد)

الان افزودنی‌ها اصلاً «افزونه وردپرس» نیستند. مکانیزم فعلی این‌طور کار می‌کند:

- `addon_add_efb()` در [functions.php](includes/functions.php) یک درخواست به API سرور وایت‌استادیو می‌زند (`/wp-json/wl/v1/addons-link/...`) و لینک دانلود zip می‌گیرد.
- `fun_addon_new()` با `download_url()` + `WP_Filesystem` + `unzip_file()` فایل را داخل `EMSFB_PLUGIN_DIRECTORY . 'vendor/<addon>'` اکسترکت می‌کند — یعنی **داخل همان پوشهٔ افزونهٔ اصلی**.
- کلاس‌ها با `require` مستقیم از بوت‌استرپ افزونهٔ اصلی لود می‌شوند (نه با مکانیزم پلاگین وردپرس، نه `activate_plugin`).
- دور همین مکانیزم، لایه‌های زیادی منطق سفارشی ساخته شده: fallback بین دو دامنه (ایران/جهانی)، retry/backoff، گیت لایسنس (`emsfb_is_addon_install_ready_efb`)، health-check محلی هر افزودنی (`is_addon_installed_locally_efb`)، و state machine ری‌کاوری بعد از آپدیت (`none/inline/block`) که در حافظهٔ قبلی هم ثبت شده (Add-on recovery UX).
- دو افزودنی (`AdnHSH` یعنی Human Shield، و fallback محلی `AdnGoS`) اصلاً دانلود نمی‌شوند و از قبل داخل خود پلاگین باندل هستند.

نتیجه: شما همین الان یک «مینی پکیج‌منیجر خصوصی» ساخته‌اید که کارش را هم خوب انجام می‌دهد. سوال شما در واقع این است که آیا این پکیج‌منیجر را با API رسمی نصب/فعال‌سازی پلاگین وردپرس جایگزین کنید یا نه.

---

## سوال ۱ — آیا وردپرس اجازه می‌دهد از صفحهٔ addons.php یک پلاگین را دانلود/نصب/فعال کند؟

**بله، کاملاً.** این دقیقاً همان الگویی است که EDD Software Licensing، افزونه‌های WooCommerce.com، ACF PRO و ده‌ها سیستم دیگر برای توزیع افزونه‌های خارج از wordpress.org استفاده می‌کنند. API لازم:

```php
require_once ABSPATH . 'wp-admin/includes/class-wp-upgrader.php';
require_once ABSPATH . 'wp-admin/includes/plugin.php';

$skin     = new Automatic_Upgrader_Skin(); // خروجی نمایشی نمی‌خواهد، برای AJAX مناسب است
$upgrader = new Plugin_Upgrader( $skin );
$result   = $upgrader->install( $zip_url ); // $zip_url = لینک مستقیم zip از سرور شما، نه wordpress.org

if ( ! is_wp_error( $result ) && $result ) {
    $plugin_file = $upgrader->plugin_info(); // مسیر نسبی فایل اصلی پلاگین
    activate_plugin( $plugin_file );
}
```

نکات مهم:

- `Plugin_Upgrader::install()` هیچ وابستگی‌ای به wordpress.org ندارد؛ فقط یک zip URL می‌خواهد. تفاوتش با دکمهٔ «Install Now» هسته این است که هسته قبلش از `plugins_api()` (که فقط w.org را می‌شناسد) لینک را resolve می‌کند — شما این مرحله را حذف می‌کنید و مستقیم لینک سرور خودتان را می‌دهید.
- پرمیشن لازم: `install_plugins` و `activate_plugins` — شما همین الان چک مشابه (`user_permission_efb_admin_dashboard`) را روی nonce `wp_rest` دارید، همان الگو قابل استفاده است.
- `WP_Filesystem`: همان چیزی که همین الان در `fun_addon_new()` استفاده می‌کنید. روی هاست‌هایی که direct method کار نمی‌کند ممکن است فرم FTP نمایش داده شود — همان محدودیتی که دکمهٔ Install Now خود وردپرس هم دارد.
- امنیت: چون zip از سرور شماست نه ریپوی رسمی، حتماً روی HTTPS + یک مکانیزم تأیید (مثلاً چک هش/امضا یا حداقل چک هدر پلاگین بعد از اکسترکت) فکر کنید، چون `Plugin_Upgrader` هیچ اعتبارسنجی محتوایی نمی‌کند.

جمع‌بندی سوال ۱: امکانش هست، ولی کار بیشتری از همین `download_url + unzip_file` فعلی‌تان می‌برد (باید `activate_plugin` و خواندن هدر پلاگین را هم اضافه کنید).

---

## سوال ۲ — یک افزونهٔ ترکیبی (`easy-form-builder-addons`) یا یک افزونهٔ جدا برای هرکدام؟

### گزینهٔ A: یک پلاگین واحد `easy-form-builder-addons` که همهٔ کلاس‌های افزودنی‌ها داخلش است

- افزونهٔ اصلی مستقیم کلاس‌ها/فایل‌های آن را صدا می‌زند — **بله این کار مشکلی ایجاد نمی‌کند**، چون PHP اهمیتی نمی‌دهد یک فایل داخل کدام پوشهٔ پلاگین است؛ فقط باید قبل از استفاده require/autoload شده باشد و namespace/کلاس تداخل نداشته باشد (namespace شما همین الان `Emsfb\...` است، پس این بخش بی‌خطر است).
- نزدیک‌ترین حالت به معماری فعلی‌تان: فقط محل دراپ‌شدن فایل‌ها از `vendor/` داخل پلاگین اصلی، به یک پلاگین خواهر منتقل می‌شود. منطق fallback/retry/backoff/health-check/license-gate همان‌طور که هست باقی می‌ماند.
- یک کلیک نصب/فعال‌سازی، همهٔ افزودنی‌ها را پوشش می‌دهد — همان UX یک‌مرحله‌ای که الان با دکمهٔ Recover دارید.

### گزینهٔ B: یک پلاگین جدا برای هر افزودنی (Stripe جدا، تلگرام جدا، ...)

- هر کدام lifecycle واقعی وردپرس دارند (غیرفعال/فعال/آپدیت مستقل)، و اگر از هدر `Requires Plugins:` (وردپرس ۶.۵+) استفاده کنید، خود وردپرس جلوی فعال‌سازی افزودنی بدون پلاگین اصلی را می‌گیرد — یک گارد رایگان.
- اما یعنی ۱۲+ هدر پلاگین، ۱۲+ نسخه‌بندی، و مهم‌تر: تمام آن منطق reliability (fallback دو دامنه، backoff، پیام انقضای لایسنس، state machine بعد از آپدیت) یا باید در هرکدام تکرار شود یا در یک کتابخانهٔ مشترک بین ۱۲ پلاگین باشد — که خودش دوباره مسئلهٔ «ترتیب لود» را N برابر می‌کند. برای چیزی که کاربر معمولاً از یک صفحهٔ تنظیمات واحد روشن/خاموش می‌کند (نه از صفحهٔ Plugins)، این سربار زیاد است در برابر فایدهٔ کم.

### توصیه

**گزینهٔ A** — یک پلاگین `easy-form-builder-addons`. نزدیک‌ترین مسیر به زیرساخت الان‌تان (که خوب سخت‌شده — تجربهٔ قبلی «گیت لایسنس آفلاین» و «Add-on recovery UX» دو نمونه از باگ‌هایی‌ست که همین مسیر فعلی را سخت کرده)، بدون تکثیر ۱۲ پلاگین جدا.

---

## چک‌لیست تغییرات لازم برای گزینهٔ A

1. **اسکلت پلاگین جدید**: `easy-form-builder-addons/easy-form-builder-addons.php` با هدر پلاگین استاندارد؛ اگر baseline وردپرس‌تان ۶.۵+ است، `Requires Plugins: easy-form-builder` اضافه کنید تا خود وردپرس گارد وابستگی را بگیرد.

2. **ترتیب لود (مهم‌ترین ریسک)**: الان ترتیب کاملاً دست خودتان است چون همه‌چیز در یک `require` sequence از یک بوت‌استرپ است (و دقیقاً همین موضوع یک بار مشکل ساز شده: یک stub سازگاری که قبل از functions.php لود می‌شد، برای همیشه helper واقعی را سایه انداخت). با دو پلاگین جدا، ترتیب لود بین پلاگین‌ها را وردپرس تعیین می‌کند (تقریباً الفبایی)، نه شما. راه‌حل: **همان الگوی lazy-require که همین الان دارید را نگه دارید** — یعنی به‌جای اتکا به `plugins_loaded`، همان `class_exists()`/`is_addon_installed_locally_efb()` را در نقطهٔ مصرف چک کنید، نه در بوت‌استرپ. این یعنی مهاجرت مکانیکی می‌شود، نه بازنویسی.

3. **جایگزینی `fun_addon_new()`**: منطق دانلود/فال‌بک/بک‌آف را نگه دارید، فقط مرحلهٔ آخر (`unzip_file` ساده) را با `Plugin_Upgrader::install()` + `activate_plugin()` عوض کنید. یک اکشن AJAX جدید کنار `ajax_recover_addons_efb` موجود اضافه کنید.

4. **ممیزی `$this->cross-class-calls`**: چون این باگ قبلاً دوبار رخ داده (یک متد از کلاس خواهر بدون ارث‌بری صدا زده شده و فاتال داده — یک بار در آپلودها، یک بار در ایمیل‌های پرداخت)، و یک مرز پلاگین جدا حساسیت بیشتری نسبت به یک زیرپوشه‌ی `vendor/` دارد، قبل از مهاجرت یک پاس توکن-اسکن روی کلاس‌های افزودنی بزنید تا فرض‌های ضمنی «همه در یک context لود شده‌اند» را پیدا کنید.

5. **امنیت دانلود**: چون این zip از سرور خودتان می‌آید نه ریپوی رسمی، یک لایهٔ تأیید (حداقل چک هدر پلاگین معتبر بعد از اکسترکت، ترجیحاً هش/امضا) اضافه کنید — `Plugin_Upgrader` به‌خودی‌خود این را انجام نمی‌دهد.

---

## جمع‌بندی کوتاه

- بله، وردپرس اجازهٔ کامل می‌دهد که از صفحهٔ addons.php یک پلاگین واقعی دانلود/نصب/فعال شود (`Plugin_Upgrader` + `activate_plugin`)، دقیقاً مثل الگوی سایر افزونه‌های پرمیوم.
- صدا زدن کلاس‌های یک پلاگین خواهر از پلاگین اصلی مشکل PHP ایجاد نمی‌کند؛ ریسک واقعی «ترتیب لود» است، نه دسترسی به کلاس‌ها — و همین الان یک الگوی lazy-check دارید که همان ریسک را خنثی می‌کند.
- توصیه: یک پلاگین ترکیبی (`easy-form-builder-addons`) به‌جای ۱۲+ پلاگین جدا، چون منطق reliability فعلی‌تان (fallback، backoff، گیت لایسنس) قابل استفادهٔ مجدد باقی می‌ماند و نیازی به تکثیر نیست.
